# Supabase Configuration
# Required: Get these values from your Supabase project dashboard
# Project Settings > API > Project URL and anon/public key

# Supabase Project ID (from project URL: https://<PROJECT_ID>.supabase.co)
VITE_SUPABASE_PROJECT_ID=your-project-id-here

# Supabase Project URL (optional, will be auto-constructed from PROJECT_ID if not provided)
VITE_SUPABASE_URL=https://your-project-id-here.supabase.co

# Supabase Anonymous/Public Key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Sentry DSN for error tracking (leave empty to disable)
VITE_SENTRY_DSN=

# Instructions:
# 1. Copy this file to .env.local (git-ignored)
# 2. Replace all placeholder values with your actual Supabase credentials
# 3. For WWS organization project, get credentials from your team lead or Supabase dashboard
# 4. Never commit .env.local to git (it's already in .gitignore)
